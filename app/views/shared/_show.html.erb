<div class="story-panel">
  <% if current_user %>
  <button class="btn btn-success btn-xs" data-toggle="modal" data-target="#invite">
   + invite friends
</button>
<% end %>
  <h2><%= @history[:title] if @history[:title] %> 
</h2>
  <div class="content"><span class="history"><%= @history[:content].html_safe %>  </span><span class="future"></span></div> 
</div>