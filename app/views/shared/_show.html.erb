<div class="story-panel">
  <button class="btn btn-success btn-xs" data-toggle="modal" data-target="#invite">
   + invite friends
</button>
  <h2><%= @history[:title] if @history[:title] %>  <% if current_user %><% end %>
</h2>
  <div class="content"><span class="history"><%= @history[:content] %> </span><span class="future"></span></div> 
</div>